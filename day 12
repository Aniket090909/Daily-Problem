#include <bits/stdc++.h> 
using namespace std; 

int fibo(int n) 
{ 
	if (n <= 1) 
	return n; 
	return fib(n - 1) + fib(n - 2); 
} 

int count(int num) 
{ 
	return fibo(num + 1); 
} 

int main() 
{ 
	int num = 4; 
	cout << "Number of ways = " << count(num);
	return 0; 
} 
